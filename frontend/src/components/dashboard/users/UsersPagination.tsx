export function UsersPagination({ page, totalPages, onPageChange }: { page: number; totalPages: number; onPageChange: (page: number) => void }) { const pages = totalPages <= 3 ? Array.from({ length: totalPages }, (_, index) => index + 1) : [1, 2, -1, totalPages]; return <footer className="flex flex-wrap items-center justify-between gap-3 border-t border-slate-200 px-1 pt-4 text-sm text-slate-500"><p>Page {page} of {totalPages}</p><div className="flex items-center gap-2"><button type="button" disabled={page === 1} onClick={() => onPageChange(page - 1)} className="rounded-lg border border-slate-200 px-3 py-2 disabled:cursor-not-allowed disabled:opacity-50">Previous</button>{pages.map((item, index) => item === -1 ? <span key={`ellipsis-${index}`} className="px-1">…</span> : <button key={item} type="button" onClick={() => onPageChange(item)} className={`size-9 rounded-lg border ${page === item ? "border-neutral-900 bg-neutral-900 text-white" : "border-slate-200 text-slate-700"}`}>{item}</button>)}<button type="button" disabled={page === totalPages} onClick={() => onPageChange(page + 1)} className="rounded-lg border border-slate-200 px-3 py-2 disabled:cursor-not-allowed disabled:opacity-50">Next</button></div></footer>; }
