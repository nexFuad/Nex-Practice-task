import { Pencil, Power, PowerOff, Trash2 } from "lucide-react";
import type { Shift } from "./types";

export function ShiftsTable({ shifts, onEdit, onStatus, onDelete }: { shifts: Shift[]; onEdit: (shift: Shift) => void; onStatus: (shift: Shift) => void; onDelete: (shift: Shift) => void }) {
  const headings = ["Name", "Code", "Color", "Time", "Duration", "Visible in roster", "Actions"];
  return <div className="w-full overflow-x-auto"><table className="min-w-[920px] w-full text-sm"><thead className="border-b border-slate-200"><tr>{headings.map((heading) => <th key={heading} className="h-10 whitespace-nowrap px-2 text-left font-medium">{heading}</th>)}</tr></thead><tbody>{shifts.length === 0 ? <tr><td colSpan={headings.length} className="h-64 text-center text-slate-500">No shifts found.</td></tr> : shifts.map((shift) => <tr key={shift.id} className="border-b border-slate-100 hover:bg-slate-50"><td className="p-2 font-medium whitespace-nowrap">{shift.name}</td><td className="p-2 whitespace-nowrap">{shift.code}</td><td className="p-2"><span className="inline-flex items-center gap-2 whitespace-nowrap"><span className="size-3 rounded-full border border-slate-300" style={{ backgroundColor: shift.color }} aria-hidden="true" />{shift.color}</span></td><td className="p-2 whitespace-nowrap">{shift.startTime} – {shift.endTime}</td><td className="p-2 whitespace-nowrap">{shift.durationHours} hrs</td><td className="p-2"><span className={`rounded-md px-2 py-0.5 text-xs font-medium ${shift.visibleInRoster ? "bg-green-100 text-green-800" : "bg-slate-100 text-slate-700"}`}>{shift.visibleInRoster ? "Visible" : "Hidden"}</span></td><td className="p-2"><div className="flex gap-1"><button type="button" onClick={() => onEdit(shift)} className="grid size-8 place-items-center rounded-md text-blue-600 hover:bg-blue-50" aria-label="Edit shift"><Pencil className="size-4" /></button><button type="button" onClick={() => onStatus(shift)} className="grid size-8 place-items-center rounded-md text-slate-600 hover:bg-slate-100" aria-label="Change roster visibility">{shift.visibleInRoster ? <PowerOff className="size-4" /> : <Power className="size-4" />}</button><button type="button" onClick={() => onDelete(shift)} className="grid size-8 place-items-center rounded-md text-red-600 hover:bg-red-50" aria-label="Delete shift"><Trash2 className="size-4" /></button></div></td></tr>)}</tbody></table></div>;
}
