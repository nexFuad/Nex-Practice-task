import { Award, BriefcaseBusiness, ContactRound, FileText, GraduationCap, ReceiptText, ScrollText, WalletCards } from "lucide-react";
import { tabs } from "./constants";
const icons = [ContactRound, BriefcaseBusiness, FileText, WalletCards, ReceiptText, Award, ScrollText, ScrollText, GraduationCap];
export type EmployeeTab = "Basic" | "Employment" | "Document" | "Payroll" | "Payslip" | "Certification" | "PWM Rank" | "KETs" | "Education";
export function EmployeeTabs({ activeTab, onChange }: { activeTab: EmployeeTab; onChange: (tab: EmployeeTab) => void }) { return <nav className="grid gap-2 rounded-xl border border-slate-200 bg-slate-50 p-2 sm:grid-cols-3 lg:grid-cols-5">{tabs.map((tab, index) => { const current = tab as EmployeeTab; const Icon = icons[index]; const available = current === "Basic" || current === "Employment" || current === "Document"; return <button key={tab} type="button" disabled={!available} onClick={() => onChange(current)} className={`flex h-10 items-center justify-center gap-2 rounded-md text-xs font-semibold uppercase tracking-wide ${activeTab === current ? "bg-neutral-950 text-white shadow" : available ? "text-slate-600 hover:bg-white" : "cursor-not-allowed text-slate-400"}`}><Icon className="size-4" />{tab}</button>; })}</nav>; }
