# Local development
NODE_ENV=development
PORT=3001
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
AUTH_JWT_SECRET=replace-with-a-long-random-secret
FRONTEND_URL=http://localhost:3000

# Railway production
# Use the exact Vercel domains that are allowed to call this API, comma-separated.
# FRONTEND_URLS=https://your-production-app.vercel.app,https://your-preview-app.vercel.app
