import { Download, RefreshCw, Search } from "lucide-react";
import type { AttendanceEmployee } from "./types";
export function AttendanceToolbar({ employees, employeeId, query, busy, onEmployee, onQuery, onRefresh, onAdd, onExport }: { employees: AttendanceEmployee[]; employeeId: string; query: string; busy: boolean; onEmployee: (value: string) => void; onQuery: (value: string) => void; onRefresh: () => void; onAdd: () => void; onExport: () => void }) {
  return <div className="space-y-4"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-2xl font-bold text-slate-900">Attendance</h2><p className="text-sm text-slate-500">Monthly attendance overview</p></div><div className="flex flex-wrap gap-2"><select value={employeeId} onChange={(event) => onEmployee(event.target.value)} className="h-8 min-w-52 rounded-md border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-400"><option value="">Select employee</option>{employees.map((employee) => <option key={employee.employeeId} value={employee.employeeId}>{employee.fullName} ({employee.employeeId})</option>)}</select><button type="button" disabled={!employeeId} onClick={onExport} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium disabled:opacity-50"><Download className="size-4" />Export XLSX</button><button type="button" disabled={!employeeId} onClick={() => window.print()} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium disabled:opacity-50"><Download className="size-4" />Export PDF</button><button type="button" onClick={onRefresh} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium hover:bg-slate-50"><RefreshCw className={`size-4 ${busy ? "animate-spin" : ""}`} />Refresh</button><button type="button" onClick={onAdd} className="h-8 rounded-md bg-blue-600 px-3 text-sm font-medium text-white shadow-sm hover:bg-blue-700">+ Add record</button></div></div><label className="relative block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search OM/Officers" className="h-9 w-full rounded-md border border-slate-200 bg-white pl-9 pr-3 text-sm outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-100" /></label></div>;
}
