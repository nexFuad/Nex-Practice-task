import { ChevronsUpDown, Download, RefreshCw, Search } from "lucide-react";
import { useEffect, useMemo, useRef, useState } from "react";
import type { AttendanceEmployee } from "./types";

export function AttendanceToolbar({ employees, employeeId, query, busy, onEmployee, onQuery, onRefresh, onAdd, onExport }: { employees: AttendanceEmployee[]; employeeId: string; query: string; busy: boolean; onEmployee: (value: string) => void; onQuery: (value: string) => void; onRefresh: () => void; onAdd: () => void; onExport: () => void }) {
  const [open, setOpen] = useState(false); const [employeeQuery, setEmployeeQuery] = useState(""); const pickerRef = useRef<HTMLDivElement>(null); const selected = employees.find((employee) => employee.employeeId === employeeId); const filtered = useMemo(() => { const value = employeeQuery.trim().toLowerCase(); return value ? employees.filter((employee) => `${employee.fullName} ${employee.employeeId}`.toLowerCase().includes(value)) : employees; }, [employeeQuery, employees]);
  useEffect(() => { if (!open) return; const close = (event: PointerEvent) => { if (!pickerRef.current?.contains(event.target as Node)) setOpen(false); }; document.addEventListener("pointerdown", close); return () => document.removeEventListener("pointerdown", close); }, [open]);
  return <div className="space-y-4"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-2xl font-bold text-slate-900">Attendance</h2><p className="text-sm text-slate-500">Monthly attendance overview</p></div><div className="flex flex-wrap gap-2"><div ref={pickerRef} className="relative min-w-52"><button type="button" aria-expanded={open} onClick={() => setOpen((value) => !value)} className="flex h-8 w-full items-center justify-between gap-2 rounded-md border border-slate-200 bg-white px-3 text-left text-sm outline-none focus:border-blue-400"><span className={`truncate ${selected ? "text-slate-800" : "text-slate-500"}`}>{selected ? `${selected.fullName} (${selected.employeeId})` : "Select employee"}</span><ChevronsUpDown className="size-4 shrink-0 text-slate-400" /></button>{open && <div className="absolute right-0 z-30 mt-2 w-80 overflow-hidden rounded-lg border border-slate-200 bg-white p-2 shadow-xl"><label className="relative block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input autoFocus value={employeeQuery} onChange={(event) => setEmployeeQuery(event.target.value)} placeholder="Search employees..." className="h-9 w-full rounded-md border border-slate-300 pl-9 pr-3 text-sm outline-none focus:border-slate-500" /></label><div className="mt-2 max-h-60 overflow-y-auto pr-1"><button type="button" onClick={() => { onEmployee(""); setOpen(false); setEmployeeQuery(""); }} className={`block w-full rounded-md px-3 py-2 text-left text-sm hover:bg-slate-100 ${!employeeId ? "bg-slate-100" : ""}`}>Select employee</button>{filtered.length ? filtered.map((employee) => <button key={employee.employeeId} type="button" onClick={() => { onEmployee(employee.employeeId); setOpen(false); setEmployeeQuery(""); }} className={`block w-full rounded-md px-3 py-2 text-left text-sm hover:bg-slate-100 ${employee.employeeId === employeeId ? "bg-slate-100" : ""}`}>{employee.fullName} ({employee.employeeId})</button>) : <p className="px-3 py-4 text-sm text-slate-500">No employees found this month.</p>}</div></div>}</div><button type="button" disabled={!employeeId} onClick={onExport} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium disabled:opacity-50"><Download className="size-4" />Export XLSX</button><button type="button" disabled={!employeeId} onClick={() => window.print()} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium disabled:opacity-50"><Download className="size-4" />Export PDF</button><button type="button" onClick={onRefresh} className="inline-flex h-8 items-center gap-1.5 rounded-md border border-slate-200 bg-white px-3 text-sm font-medium hover:bg-slate-50"><RefreshCw className={`size-4 ${busy ? "animate-spin" : ""}`} />Refresh</button><button type="button" onClick={onAdd} className="h-8 rounded-md bg-blue-600 px-3 text-sm font-medium text-white shadow-sm hover:bg-blue-700">+ Add record</button></div></div><label className="relative block"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search OM/Officers" className="h-9 w-full rounded-md border border-slate-200 bg-white pl-9 pr-3 text-sm outline-none focus:border-blue-400 focus:ring-2 focus:ring-blue-100" /></label></div>;
}
